spring:
  application:
    name: browser-scheduler
  jmx:
    enabled: false
    unique-names: true
   
management:
  endpoints:
    enabled-by-default: false

browser-scheduler:
  instanceName: BrowserSchedulerTest
  jobParallelism: 2
